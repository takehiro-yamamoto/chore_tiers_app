<div class="stat-card recent-logs-col">
  <h3 class="text-[#5D4037] text-xl font-semibold mb-4">最近の家事完了履歴</h3>

  <% if @completion_logs.any? %>
    <ul class="space-y-3">
      <% @completion_logs.each do |log| %>
        <li class="recent-log-item">
          <span class="chore-name"><%= log.chore.title %>（<%= log.user.name %>）</span>
          <span class="completed-time">
            <%= log.completed_at.in_time_zone("Asia/Tokyo").strftime("%-m月%-d日 %H:%M") %> に完了 ✅
          </span>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p class="text-sm text-[#7A6C5D]">まだ完了履歴はありません。</p>
  <% end %>
</div>
